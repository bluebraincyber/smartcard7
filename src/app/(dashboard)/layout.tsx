// Layout removido - usando /dashboard/layout.tsx como principal
// Este arquivo foi mantido apenas para compatibilidade
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return children;
}
