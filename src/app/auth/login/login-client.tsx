// Arquivo temporariamente removido para evitar erros de compilação
// Este será recriado após resolver o problema de build
export function LoginClient() {
  return <div>Login temporariamente indisponível</div>
}
